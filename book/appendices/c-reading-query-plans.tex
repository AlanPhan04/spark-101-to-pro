\chapter{Reading Query Plans}
\label{app:plans}

\tbd

% A cheat-sheet, usable standalone after first read: every physical operator the
% book mentions (Scan, Filter, Project, Exchange, Sort, *HashAggregate,
% BroadcastHashJoin, SortMergeJoin, ShuffledHashJoin, BroadcastExchange,
% AQEShuffleRead, ...), what it means, what typically precedes it, and the
% annotations to look for (PartitionFilters, PushedFilters, `*(n)` codegen
% markers, isFinalPlan, isSkew). Cross-referenced to Chapter 2.
