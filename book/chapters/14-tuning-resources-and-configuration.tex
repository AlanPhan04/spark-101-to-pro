\chapter{Tuning Resources and Configuration}
\label{ch:tuning}

\begin{chapterintro}
This chapter does not hand you a table of recommended settings. It derives an
executor shape from the memory map in \Cref{ch:execmem} and the shuffle in
\Cref{ch:shuffle}, so that every number --- cores, heap, overhead, partition
count --- has a reason you can defend. It then covers dynamic allocation, the
external shuffle service on Kubernetes, configuration precedence, and running
Spark behind Kyuubi for many users. For the standalone list of tuning knobs with
values, see \texttt{spark\_memory\_tuning.md} in the repository root.
\end{chapterintro}

\section{Executor sizing from the mechanism}
\label{sec:executor-sizing}

Sizing an executor is choosing three numbers --- \conf{spark.executor.cores},
\conf{spark.executor.memory}, \conf{spark.executor.memoryOverhead} --- and
each has a mechanism from an earlier chapter that bounds it, not a rule of
thumb.

\textbf{Cores per executor} sets how many tasks run concurrently inside one
JVM, which sets how many task-sized shares the unified memory pool from
\Cref{sec:mem-model} gets divided into --- more cores per executor means each
concurrent task's share of execution memory is smaller, which means a join
or aggregation that was comfortable at four cores can start spilling at
eight with no other change. Five cores is the commonly cited ceiling on
this stack's Kubernetes deployment, not because of memory but because the
GCS connector's own per-JVM connection pool degrades under too many
concurrent requests from one process --- the same throughput ceiling
originally observed against HDFS clients, and one that applies here for the
identical reason: one executor JVM, one shared client, one pool, regardless
of which object store sits behind it. There is no memory-model reason to
stop earlier than five if the workload reads little from GCS directly (all
its input already cached, say); the ceiling is specifically about
concurrent storage-client requests, not about anything this section's
memory arithmetic bounds.

\textbf{Executor memory} is sized backward from the largest single operation
you expect a task to run --- a sort-merge join's sort buffer, a hash
aggregation's hash table --- divided by the fraction of unified memory one
task actually gets (execution memory divided evenly among concurrently
running tasks, \Cref{sec:mem-model}), then inflated to account for
\conf{spark.memory.fraction}'s default 60\% split between unified and
reserved-plus-user memory. Guessing low here is what \Cref{sec:reading-metrics}'s
spill metrics exist to catch.

\textbf{Memory overhead} (\conf{spark.executor.memoryOverhead}, default the
greater of 10\% of executor memory or 384~MB) covers everything outside the
JVM heap that the container still needs: off-heap Tungsten memory if enabled,
JVM metaspace and thread stacks, and --- specific to this stack ---
PySpark's separate Python process when UDFs are in use. The formula is a
floor, not a measurement: 384~MB covers the JVM's own fixed costs (metaspace,
thread stacks) on a small executor where 10\% would be too little, and 10\%
scales that floor up for a large executor where metaspace stays roughly
fixed but off-heap Tungsten buffers and any PySpark worker grow with it.
Neither number is derived from anything about \emph{your} workload, which is
exactly why a container OOM kill with no corresponding Java heap error
(\Cref{sec:failure-signatures}) is almost always this default proving too
small for what is actually running outside the heap --- raise it directly
rather than trusting the formula once PySpark UDFs or off-heap execution
memory are in use.

\paragraph{A worked shape for the specimen.} \Cref{sec:shape-jobs}'s stage~2
runs the specimen's sort-merge join and hash aggregate across 200 shuffle
partitions; at the reference cluster's 4 cores per executor
(\Cref{app:refenv}), each core is one task slot, so up to 4 of those 200
partitions execute concurrently per executor. If the join's build-side
partition (post-shuffle, decoded) runs 300~MB at its largest --- the viral
product's oversized partition from \Cref{sec:shape-ui} --- each concurrent
task needs that much execution memory, and \Cref{sec:mem-model}'s unified
pool splits evenly across the 4 concurrent tasks: $4 \times 300\text{MB} =
1.2\text{GB}$ of execution memory alone. At \conf{spark.memory.fraction}'s
default 0.6 unified share, that implies roughly $1.2\text{GB} / 0.6 \approx
2\text{GB}$ of heap just to keep that one partition from spilling, before
adding headroom for the rest of a normal working set --- a defensible
starting point of \conf{spark.executor.memory=4g}, with
\conf{spark.executor.memoryOverhead} left at its 10\% default (409.6~MB)
since no PySpark UDF or off-heap pool is in play here.

\begin{tip}
On Kubernetes, the pod's memory limit is executor memory plus overhead plus
(if set) off-heap size --- exceeding it kills the pod outright with no JVM
exception to read, which is why \Cref{sec:mem-model}'s "container
limit" bracket exists as a distinct concept from the heap it contains.
\end{tip}

\section{Dynamic allocation}
\label{sec:dynamic-allocation}

A fixed executor count is either wasted (idle executors between jobs in a
long-lived session) or insufficient (a burst of work with no room to grow).
\conf{spark.dynamicAllocation.enabled} lets Spark request and release
executors against the cluster manager as work arrives and drains: it adds
executors when the scheduler's pending-task backlog persists past
\conf{spark.dynamicAllocation.schedulerBacklogTimeout} (1s default), and
removes an executor once it has been idle past
\conf{spark.dynamicAllocation.executorIdleTimeout} (60s default), bounded by
\conf{spark.dynamicAllocation.minExecutors} and \texttt{.maxExecutors}.

The one condition that makes this safe is exactly the failure mode
\Cref{sec:failure-signatures} described: an executor holding shuffle files
that a later stage still needs cannot simply be released. Without an
external shuffle service, Spark tracks this via
\conf{spark.dynamicAllocation.shuffleTracking.enabled} (on by default since
Spark 3.0), which keeps an executor from being removed while its shuffle
output is still referenced; with an external service
(\Cref{sec:shuffle-external}), the files outlive the executor entirely and
removal is unconstrained by this concern.

\section{Parallelism defaults}
\label{sec:tuning-parallelism}

\conf{spark.default.parallelism} governs RDD operations with no other hint
(defaulting to total core count across executors); for DataFrame/SQL
workloads, \conf{spark.sql.shuffle.partitions} (\Cref{sec:shuffle-sizing},
\Cref{sec:aqe-coalesce}) is the number that actually matters on this stack.
The one addition worth making here rather than in either earlier chapter:
this default and AQE's coalescing are a \emph{resource-sizing} lever as much
as a shuffle one --- the same 200-versus-2000-partitions choice that trades
task-scheduling overhead for spill risk also decides how many of the
executor shape's task slots a given stage can actually fill, so size
partitions and executors together, not as two independent decisions.

\section{The external and push-based shuffle service}
\label{sec:shuffle-external}

Without an external shuffle service, a reduce task fetches shuffle blocks
directly from the executor that wrote them (\Cref{sec:shuffle-write}) ---
which is exactly the dependency dynamic allocation has to work around above,
and exactly what turns a voluntary executor removal into a
\texttt{FetchFailedException} if the removal races a still-pending fetch.
\conf{spark.shuffle.service.enabled} moves block serving out of the executor
JVM and into a separate long-lived process, so shuffle data remains
servable after the executor that wrote it is gone --- dynamic allocation
can then scale down aggressively with no data-loss risk, because the data
was never tied to the executor's own lifecycle. Push-based shuffle
(\Cref{sec:shuffle-push}) builds on the same external service to also cut
fetch-side amplification.

\begin{warning}
On YARN, the external shuffle service is a NodeManager auxiliary service
available on every node by default. On Kubernetes it is not: it has to be
deployed and run explicitly, as a DaemonSet with one pod per node, before
\conf{spark.shuffle.service.enabled} does anything --- setting the config
alone with no DaemonSet running leaves executors unable to reach a shuffle
service at all, which surfaces as fetch failures indistinguishable from the
problem the config was meant to prevent. Confirm the DaemonSet is actually
running on the cluster before relying on this setting.
\end{warning}

\section{Configuration precedence and profiles}
\label{sec:tuning-precedence}

A given key can be set in more than one place, and Spark resolves conflicts
in a fixed order, highest priority first: values set programmatically on a
\texttt{SparkConf} in application code; \texttt{--conf} flags (or
\texttt{--executor-memory}-style shortcuts) passed to \texttt{spark-submit};
values in \texttt{spark-defaults.conf} on the client; and finally each
setting's built-in default. A value that looks ignored is almost always
being overridden from a higher-priority layer than the one you edited ---
checking \texttt{spark.conf.get(key)} inside a running session, which reports
the value actually in effect, settles this faster than re-reading config
files.

\section{Kyuubi and multi-tenancy}
\label{sec:tuning-kyuubi}

Kyuubi (this stack's gateway) runs one Spark application per Kyuubi
\emph{engine} rather than one per user's individual query, and its
\emph{share level} setting decides how those engines are pooled and how a
new connection finds its engine. Mechanically, Kyuubi's server process
keeps a registry of live engines keyed by a scope derived from the share
level --- a connection id for \texttt{CONNECTION}, a username for
\texttt{USER}, a constant for \texttt{SERVER} --- backed by Zookeeper (or
etcd) so the registry survives a Kyuubi server restart and multiple Kyuubi
server instances see the same engines. A new JDBC connection computes its
key, looks it up in that registry, and either attaches to the engine
already running under that key or, on a miss, launches a fresh Spark
application and registers it under that key for the next connection to
find. \texttt{CONNECTION} gives every JDBC connection its own engine
(maximum isolation, maximum startup latency per query, since every
connection is a registry miss); \texttt{USER} shares one engine across all
of a user's connections (a warm, persistent Spark session per user, a
registry hit for every connection after the first); \texttt{SERVER} shares
a single engine across every user on the Kyuubi instance (cheapest, but one
user's job competing for the same executors as everyone else's). An
\emph{engine pool} pre-registers several engines under a share level before
any connection asks for one, so even a first connection under that scope is
a registry hit rather than a cold start. All of this sits above the
resource-sizing questions from this chapter's earlier sections, not in
place of them --- each pooled engine is still an ordinary Spark application
whose executors are sized exactly as described above.

\section{Summary}
\label{sec:tuning-summary}

Executor cores, memory, and overhead each trace back to a mechanism from
earlier chapters --- concurrent tasks dividing the unified memory pool,
the largest operation's actual memory need, and everything living outside
the JVM heap, respectively --- rather than a rule of thumb. Dynamic
allocation is safe to scale aggressively once shuffle data no longer lives
and dies with the executor that produced it, which the external (and,
further, push-based) shuffle service guarantees. Configuration resolves in a
fixed precedence order regardless of how many places a key is set, and
Kyuubi's engine share level is a pooling decision layered on top of, not
instead of, ordinary executor sizing.

\exercisehead
\begin{exercises}
\item Take a job that spills. Propose two different fixes --- more memory versus
      fewer cores --- and predict which the mechanism favours, and why.
\item Given a 32~GB, 8-core node, propose an executor shape (cores, memory,
      overhead) and justify the core count from the connection-pool
      consideration in \Cref{sec:executor-sizing}.
\item Enable dynamic allocation without the external shuffle service on a
      job with a long-running final stage. Predict, then observe, what
      happens when an idle executor holding needed shuffle data is
      considered for removal.
\end{exercises}
